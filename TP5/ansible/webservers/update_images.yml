- name: Webservers configuration
  remote_user: ubuntu
  become: true
  hosts: webservers
  serial: 1
  gather_facts: no
  tasks:
    - name: Import webservers tasks
      include_tasks: tasks/webservers_tasks.yml